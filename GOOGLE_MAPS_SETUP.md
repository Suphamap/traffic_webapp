# Google Maps API Setup Instructions

## คำแนะนำการติดตั้ง Google Maps API

### ขั้นตอนที่ 1: สร้าง Google Cloud Project
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจ็กต์ใหม่หรือเลือกโปรเจ็กต์ที่มีอยู่
3. เปิดใช้งาน Google Maps JavaScript API

### ขั้นตอนที่ 2: สร้าง API Key
1. ไปที่ "Credentials" ในเมนู "APIs & Services"
2. คลิก "Create Credentials" > "API Key"
3. คัดลอก API Key ที่สร้างขึ้น

### ขั้นตอนที่ 3: จำกัดการใช้งาน API Key (แนะนำ)
1. คลิกที่ API Key ที่สร้างขึ้น
2. ใน "Application restrictions" เลือก "HTTP referrers"
3. เพิ่ม domain ของเว็บไซต์ (เช่น `localhost:*`, `yourdomain.com/*`)
4. ใน "API restrictions" เลือก "Restrict key" และเลือก:
   - Maps JavaScript API
   - Geolocation API (ถ้าต้องการ)

### ขั้นตอนที่ 4: แทนที่ API Key ในโค้ด
1. เปิดไฟล์ `traffic-map.html`
2. หา `YOUR_API_KEY` ในบรรทัด:
   ```html
   <script async defer 
       src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap&libraries=geometry">
   </script>
   ```
3. แทนที่ `YOUR_API_KEY` ด้วย API Key ที่คุณสร้างขึ้น

### ขั้นตอนที่ 5: ทดสอบ
1. เปิดเว็บไซต์ในเบราว์เซอร์
2. คลิกปุ่ม "พยากรณ์เลย"
3. อนุญาตการเข้าถึงตำแหน่งเมื่อเบราว์เซอร์ถาม
4. แผนที่จะแสดงตำแหน่งปัจจุบันและข้อมูลการจราจรแบบเรียลไทม์

## ฟีเจอร์ที่มีอยู่:
- 📍 ระบุตำแหน่งผู้ใช้อัตโนมัติ
- 🚦 แสดงข้อมูลการจราจรแบบเรียลไทม์
- 🗺️ เปลี่ยนประเภทแผนที่ (ถนน, ดาวเทียม, ผสม, ภูมิประเทศ)
- 📱 รองรับการใช้งานบนมือถือ
- 🔄 อัปเดตตำแหน่งแบบเรียลไทม์
- 🎯 ค้นหาตำแหน่งใหม่
- ⚠️ การจัดการข้อผิดพลาด

## หมายเหตุ:
- Google Maps API มีข้อจำกัดการใช้งานฟรี
- ควรตั้งค่าข้อจำกัดการใช้งาน API Key เพื่อความปลอดภัย
- การใช้งานบนโดเมน localhost ไม่เสียค่าใช้จ่าย
